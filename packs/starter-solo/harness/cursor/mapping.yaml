# 槽位 id → Cursor subagent 文件名（不含 .md）

roster_id: starter-solo

mappings:
  builder: solo-builder
  reviewer: solo-reviewer
  progress: progress-keeper
  auditor: quality-auditor

# 可选：覆盖 agent frontmatter 的 model 字段（见 model-harness-contract.md）
model_hints:
  auditor: inherit
  reviewer: fast

# 主 agent 使用项目默认 Agent；或指定：
# parent: default
